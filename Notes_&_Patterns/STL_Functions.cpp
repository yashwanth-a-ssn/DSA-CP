#include <iostream>
#include <bits/stdc++.h>
using namespace std;

// To Find Min and Max Element in a Vector
// int min = *min_element(nums.begin(), nums.end());
// int max = *max_element(nums.begin(), nums.end());
// Must use Dereference Operator (*)



// Integer Arithmetic Alternative for ceil()
// ceil(a / b) = (a + b - 1) / b



// To Find of Elements in a Vector
// accumulate(nums.begin(), nums.end(), 0);


// To Avoid Integer Overflow while finding Mid Value
// mid = (low +hight) / 2 = low + (high - low) / 2;

int main(){
    // Main Function placeholder for testing
    return 0;
}